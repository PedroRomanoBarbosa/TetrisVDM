class TetrisTest

	operations
    public static main: () ==> int
    main() ==
    (
    	dcl tetris: Table := new Table(5,5);
    	-- Add pieces
    	--tetris.newPiece();
    	--tetris.newPiece();
    	--tetris.newPiece();
    	--tetris.newPiece();
    	return tetris.getRandom();
    );
    
    traces
    
end TetrisTest