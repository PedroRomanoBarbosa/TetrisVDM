class TetrisTest

	operations
    public static main: () ==> ()
    main() ==
    (
    	dcl tetris: Table := new Table(5,5);
    	-- Add pieces
    	--tetris.newPiece();
    	--tetris.newPiece();
    	--tetris.newPiece();
    	--tetris.newPiece();
    	IO`print(tetris.getRandomPieceType());
    );
    
    traces
    
end TetrisTest