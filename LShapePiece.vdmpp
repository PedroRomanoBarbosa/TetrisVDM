class LShapePiece is subclass of PivotPiece

instance variables

operations

	-- OOO
	-- XOO
	-- XXX
	public LShapePiece: int * int * char * nat1 ==> LShapePiece
	LShapePiece(x, y, s, r) == (
		dcl t: Sprite := [mk_Coordinate(-1,0), mk_Coordinate(-1,1), mk_Coordinate(0,1), mk_Coordinate(1,1)];
		PivotPiece(x, y, s, r, t);
	);
	
end LShapePiece