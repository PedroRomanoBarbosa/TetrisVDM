class LShapePiece is subclass of PivotPiece

instance variables

operations

	-- OOO
	-- XOO
	-- XXX
	public LShapePiece: int * int * char ==> LShapePiece
	LShapePiece(x, y, s) == (
		dcl t: Sprite := [mk_Coordinate(-1,-1), mk_Coordinate(-1,0), mk_Coordinate(0,0), mk_Coordinate(1,0)];
		PivotPiece(x, y, s, t);
	);
	
end LShapePiece