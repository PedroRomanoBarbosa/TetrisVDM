class TetrisTest

	operations
    public static main: () ==> ()
    main() == (
    	dcl tetris: Tetris := new Tetris(15, 10);
    	
  		dcl sp: Piece`Sprite;
			tetris := new Tetris(15, 10);
  		tetris.init();
  		tetris.print();
  		
  		--tetris.rotate();
  		tetris.rotate();
  		tetris.print();
  		
  		--tetris.rotate();
  		tetris.rotate();
  		tetris.print();
  		
  		--tetris.rotate();
  		tetris.rotate();
  		tetris.print();
    );
    
end TetrisTest