class TetrisTest

	operations
    public static main: () ==> ()
    main() ==
    (
    	dcl tetris: Table := new Table(15,10);
    	dcl l: Piece := new LShapePiece(1,1,'X');
    	-- Add pieces
    	--tetris.newPiece();
    	--tetris.newPiece();
    	--tetris.newPiece();
    	--tetris.newPiece();
    	tetris.setCurrentPiece(l);
    	IO`println(l);
    	tetris.paintCurrentPiece();
    	tetris.print();
    );
    
end TetrisTest