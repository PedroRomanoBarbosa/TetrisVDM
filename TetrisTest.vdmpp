class TetrisTest

	instance variables
		s : Table := new Table(5,5);
		var : nat := 0;
		
	operations
    private assertTrue: bool ==> ()
    assertTrue(cond) == return
    pre cond;
            
    private testTable: () ==> ()
    testTable() ==
    (
 			var := s.print();
    );
    
    public static main: () ==> ()
    main() ==
    (
    	new TetrisTest().testTable();   
    );
    
    traces
    
end TetrisTest