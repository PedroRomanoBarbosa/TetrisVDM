class TetrisTest

	operations
    public static main: () ==> ()
    main() ==
    (
    	dcl tetris: Table := new Table(15,10);
    	dcl l: Piece := new LShapePiece(5,14,'L');
    	dcl l2: Piece := new LShapePiece(4,4,'L');
    	tetris.setCurrentPiece(l2);
    	tetris.setCurrentPiece(l);
    	IO`println(tetris.checkCurrentPieceDownCollision());
    	tetris.print();
    );
    
end TetrisTest